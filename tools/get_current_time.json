{
  "name": "get_current_time",
  "code": "def get_current_time(location, **kwargs):\n    from datetime import datetime\n    return f\"Current time request for {location}. Actual time: {datetime.now().strftime('%I:%M %p')}\"",
  "description": "Get the current time for a given location.",
  "parameters": {
    "location": {
      "type": "string",
      "description": "The location to get the time for.",
      "required": true
    }
  }
}
