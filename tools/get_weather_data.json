{
  "name": "get_weather_data",
  "code": "def get_weather_data(location, unit='Celsius', **kwargs):\n    import json\n    weather_data = {\n        \"temperature-high\": f\"30 {unit}\",\n        \"temperature-low\": f\"20 {unit}\",\n        \"sky\": \"Clear\",\n        \"rains\": \"Slight Possibility\"\n    }\n    return json.dumps(weather_data)",
  "description": "Get weather and temperature data for a location.",
  "parameters": {
    "location": {
      "type": "string",
      "description": "The location to get weather data for.",
      "required": true
    },
    "unit": {
      "type": "string",
      "description": "Unit of temperature — celsius or fahrenheit.",
      "required": false
    }
  }
}
