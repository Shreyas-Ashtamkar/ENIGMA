{
  "name": "send_email",
  "code": "def send_email(recipient, subject=None, body=None, **kwargs):\n    print(f\"CALLED: send_email\\nTO: {recipient}\\nSUBJECT: {subject}\\nBODY: {body}\")\n    return f\"Successfully sent email to {recipient} with subject '{subject}'\"",
  "description": "Send an email with a subject and body to a recipient.",
  "parameters": {
    "recipient": {
      "type": "string",
      "description": "The recipient's email address.",
      "required": true
    },
    "subject": {
      "type": "string",
      "description": "The subject line of the email.",
      "required": false
    },
    "body": {
      "type": "string",
      "description": "The body content of the email.",
      "required": false
    }
  }
}
