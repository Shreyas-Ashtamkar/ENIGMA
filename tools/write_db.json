{
  "name": "write_db",
  "code": "def write_db(query, **kwargs):\n    return f\"Executed {query}\"",
  "description": "Write to a database.",
  "parameters": {
    "query": {
      "type": "string",
      "description": "The database query to execute.",
      "required": true
    }
  }
}
