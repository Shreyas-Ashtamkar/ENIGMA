{
  "name": "read_db",
  "code": "def read_db(query, **kwargs):\n    return f\"Results for {query}\"",
  "description": "Read from a database.",
  "parameters": {
    "query": {
      "type": "string",
      "description": "The database query to execute.",
      "required": true
    }
  }
}
